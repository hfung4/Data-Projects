from survey.config.core import PACKAGE_ROOT

print("initializing the survey package")

#with open(PACKAGE_ROOT / "VERSION") as version_file:
#    __version__ = version_file.read().strip()

#print(f"The version is {__version__}")

num_of_houses = 5